import { PrismaClient } from "@prisma/client";\nimport { PrismaBetterSqlite3 } from "@prisma/adapter-better-sqlite3";\n\nconst adapter=new PrismaBetterSqlite3({url:"file:./dev.db"});\nconst prisma=new PrismaClient({adapter});\n\nconst users=await prisma.user.count();\nconst reservations=await prisma.reservation.count();\nconst pendingUsers=await prisma.user.count({where:{status:"PENDING"}});\nconst pendingReservations=await prisma.reservation.count({where:{status:"PENDING"}});\n\nconsole.log(JSON.stringify({users,reservations,pendingUsers,pendingReservations}));\nawait prisma["$disconnect"]();\n
